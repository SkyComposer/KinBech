<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/post_ad" android:icon="@android:drawable/ic_menu_camera"
            android:title="Post Ad" />
        <item android:id="@+id/view_ad" android:icon="@android:drawable/ic_menu_gallery"
            android:title="View Ad" />
        <item android:id="@+id/profile" android:icon="@android:drawable/ic_menu_slideshow"
            android:title="Profile" />
        <item android:id="@+id/settings" android:icon="@android:drawable/ic_menu_manage"
            android:title="Settings" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item android:id="@+id/about_app" android:icon="@android:drawable/ic_menu_send"
                android:title="about_app" />
            <item android:id="@+id/logout" android:icon="@android:drawable/ic_menu_send"
                android:title="Logout" />
        </menu>
    </item>

</menu>